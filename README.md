# Бэкенд для сервиса пеших прогулок "FootPath" - Монолитная версия

## Обзор проекта

**FootPath Monolith** — это монолитная версия бэкенда для мобильного приложения, предназначенного для автоматического составления персонализированных пешеходных маршрутов. Сервис решает проблему отсутствия актуальных данных о прогулочных местах в России на картах типа OpenStreetMap, позволяя пользователям самим добавлять и верифицировать точки интереса (POI).

Система включает в себя аутентификацию с ролевой моделью, социальные функции (друзья, приглашения на совместные прогулки) и умную рекомендательную систему, которая подбирает маршруты на основе интересов и предпочтений пользователя.

## Архитектура системы

В отличие от микросервисной архитектуры, этот проект представляет собой **монолитное приложение**, где все функциональные компоненты объединены в единой NestJS-приложение. Такой подход позволяет существенно ускорить разработку MVP и упрощает развёртывание и управление системой.

*   **Единая точка входа:** Все HTTP-запросы обрабатываются одним приложением через API Gateway (реализованный как часть монолита).
*   **Интеграция баз данных:** Вместо использования разных баз данных для каждого сервиса, монолит использует:
    - **PostgreSQL + PostGIS** - основная реляционная база данных с поддержкой геоинформационных данных
    - **Neo4j** - графовая база данных для хранения социальных связей
*   **Внутренняя логика:** Все бизнес-логика, включая рекомендательную систему, реализована в едином приложении
*   **Консистентность данных:** Данные остаются согласованными благодаря использованию одной системы транзакций

### Визуальная схема

```mermaid
graph TD
    subgraph "Клиент"
        MobileApp[📱<br>Мобильное приложение<br>(OpenStreetMap)]
    end

    subgraph "Монолитное приложение"
        Monolith[NestJS Monolith<br>Все службы в одном приложении]
        PG[(🐘<br>PostgreSQL + PostGIS)]
        Neo4j[(🌐<br>Neo4j)]
        
        Monolith <--> PG
        Monolith <--> Neo4j
    end

    subgraph "Прочие компоненты"
        Redis[(⚡<br>Redis<br>(кеширование, сессии)]
    end

    %% --- Connections Flow ---
    MobileApp -->|HTTP API| Monolith
    Monolith -->|Кеширование сессий| Redis
```

## Ключевые возможности

*   **Аутентификация и роли:** Регистрация и вход по email/паролю, JWT-аутентификация, ролевая модель (`user`, `moderator`, `admin`).
*   **Управление местами (POI):** Пользователи могут добавлять точки интереса на карту, которые проходят модерацию перед публикацией.
*   **Социальные функции:** Система друзей, отправка запросов, просмотр списка друзей, приглашение друзей на прогулки.
*   **Генерация маршрутов:** Пользователь задает точку старта, конца (опционально), длительность и теги (например, "романтика", "с детьми"). Система строит оптимальный маршрут, учитывая время нахождения в каждом месте и время ходьбы между ними.
*   **Персональные рекомендации:** Рекомендательная система предлагает пользователям новые места на основе их предыдущих прогулок и интересов.
*   **Уведомления:** Push-уведомления о важных событиях (запросы в друзья, приглашения на прогулку, статус модерации места).
*   **Подробное логирование:** Вся активность системы логируется для отладки и мониторинга.

## Технологический стек

| Категория | Технология | Назначение |
| :--- | :--- | :--- |
| **Бэкенд-фреймворк** | **NestJS (Node.js)** | Основной фреймворк для реализации всего функционала. |
| **Кеширование и сессии** | **Redis** | Кеширование и хранение сессий. |
| **Основная база данных** | **PostgreSQL** | Основная реляционная БД для большинства сущностей. |
| | **PostGIS** | Расширение для `PostgreSQL` для хранения геоданных. |
| | **pgvector** | Расширение для `PostgreSQL` для векторного поиска (рекомендации). |
| | **Neo4j** | Графовая БД для хранения социальных связей. |
| **ML/Рекомендации**  | **NestJS (Node.js)**  | библиотека для системы рекомендаций |

## Структура проекта

```
footpath-backend-monolith/
├── src/
│   ├── auth/                 # Аутентификация и профили
│   ├── places/               # Управление местами
│   ├── paths/                # Управление маршрутами
│   ├── friends/              # Социальные функции
│   ├── walks/                # Управление прогулками
│   ├── notifications/        # Уведомления
│   ├── recommendations/      # Рекомендательная система
│   ├── shared/               # Общие утилиты и сущности
│   └── app.module.ts         # Главный модуль приложения
├── docker-compose.yml        # Конфигурация для запуска всего стека
├── Dockerfile                # Docker-файл для монолитного приложения
├── README.md                 # Документация
└── docs/                     # Дополнительная документация
```

## Спецификация REST API

Все запросы должны содержать заголовок `Authorization: Bearer <jwt_token>` (кроме эндпоинтов регистрации/логина).

### Единый интерфейс Swagger
Приложение предоставляет единый интерфейс Swagger (OpenAPI) по адресу `/api/docs`, который предоставляет:
- Единый обзор всех доступных API-методов
- Возможность тестирования API-методов в интерактивном режиме
- Документацию по каждому из эндпоинтов
- Информацию о форматах запросов и ответов
- Интеграцию с JWT-аутентификацией для тестирования защищенных эндпоинтов

*   **Аутентификация (`/auth`)**
    *   `POST /register`: Регистрация нового пользователя.
    *   `POST /login`: Вход, получение JWT.
    *   `GET /me`: Получение информации о себе.
    *   `PUT /profile`: Обновление профиля пользователя.
    *   `POST /request-password-reset`: Запрос сброса пароля.
    *   `POST /reset-password`: Сброс пароля по токену.

*   **Места (`/places`)**
    *   `POST /`: Добавить новое место на модерацию.
    *   `GET /`: Поиск мест в радиусе.
    *   `GET /{id}`: Получить информацию о месте.
    *   `PUT /{id}/approve`: (Модератор) Одобрить место.

*   **Друзья (`/friends`)**
    *   `GET /`: Получить список друзей.
    *   `POST /requests`: Отправить запрос в друзья.
    *   `POST /requests/{id}/accept`: Принять запрос.
    *   `DELETE /{userId}`: Удалить из друзей.

*   **Прогулки (`/walks`)**
    *   `POST /generate`: Сгенерировать маршрут по параметрам.
    *   `POST /`: Создать прогулку на основе сгенерированного маршрута.
    *   `POST /{id}/invite`: Пригласить друга на прогулку.
    *   `POST /{id}/complete`: Завершить прогулку.

*   **Рекомендации (`/recommendations`)**
    *   `GET /places`: Получить список рекомендованных мест.

## Реализация рекомендательной системы

Рекомендательная система использует гибридный подход для достижения наилучших результатов, реализованный внутри монолита.

1.  **Сбор данных:** Система отслеживает завершенные прогулки и посещенные места внутри одного приложения.
2.  **Content-Based Filtering:** Система рекомендует места с тегами, похожими на теги мест, которые пользователь уже посещал.
3.  **Векторные представления (Embeddings):**
    *   Когда новое место одобряется, скрипт генерирует для его текстового описания и тегов числовой **вектор (эмбеддинг)** с помощью ML-модели.
    *   Этот вектор сохраняется в PostgreSQL с расширением `pgvector`.
    *   При запросе рекомендаций система может найти семантически похожие места, выполнив поиск ближайших векторов в базе данных.

## Локальный запуск

Для развертывания проекта локально потребуется [Docker](https://www.docker.com/) и [Docker Compose](https://docs.docker.com/compose/).

1.  **Клонировать репозиторий:**
    ```bash
    git clone <repository_url>
    cd <repository_name>
    ```

2.  **Настроить переменные окружения:**
    Создайте файл `.env` на основе файла `.env.example`.

3.  **Запустить приложение:**
    ```bash
    docker-compose up -d --build
    ```
4.  API будет доступен по адресу `http://localhost:3000`.

## Production Deployment

Для развертывания в production-окружении выполните следующие шаги:

1.  **Скопируйте файлы развертывания:**
    ```bash
    # Убедитесь, что у вас есть .env файл с production-параметрами
    cp .env.example .env
    # Отредактируйте .env файл с production-параметрами
    ```

2.  **Запустите сценарий production-развертывания:**
    ```bash
    ./setup-prod.sh
    ```

Или выполните вручную:
```bash
docker-compose -f docker-compose.prod.yml up -d --build
```

API будет доступен по адресу `http://your-server-ip:3000`, а документация API - по адресу `http://your-server-ip:3000/api/docs`.

Для подробного руководства по развертыванию см. [DEPLOYMENT.md](DEPLOYMENT.md).